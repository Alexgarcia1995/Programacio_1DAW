

import java.util.Comparator;

import modules.user.classes.*;

public class Orderbenefits implements Comparator <Persona>{

	public int compare (User u1, User u2) {
		if(u1.getBenefits()>u2.getBenefits())
			return 1;
		if(u1.getBenefits()<u2.getBenefits())
			return -1;
		return 0;
	}
}
